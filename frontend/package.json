{
  "name": "asset-tracker-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"No build step (static HTML)\"",
    "build:azure": "echo \"No build step (static HTML)\""
  }
}
